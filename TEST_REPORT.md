# MVP 验收记录

界面优化：移除列表“日志”列，通过脚本选择切换日志；新增 `std` / `stdout` / `stderr` 下拉筛选，默认显示全部日志。自动化测试 46 项通过，包括筛选历史 / 实时日志、切换脚本保留筛选、切回全部日志；布局已在隔离渲染中检查。新版输出 `dist/ui-update/ScriptDock.exe`。

后续修复：托盘新增单击打开管理器，保留双击及右键菜单行为。当前自动化测试 45 项通过，单击 / 双击生命周期检查通过；新版输出 `dist/update/ScriptDock.exe`，不覆盖运行中的旧版。

验收日期：2026-09-16。环境：Windows 11 x64，Python 3.12.14，PySide6 6.11.2，psutil 7.2.2，PyInstaller 6.22.3。

结果：`python -m pytest -q` 40 项全部通过，连续复跑两次通过。`tools/verify_lifecycle.py` 隔离生命周期检查通过。最终 `dist/ScriptDock.exe` 已以普通交互式方式启动，实际主窗口可见，PE 子系统为 Windows GUI（2），无控制台。单文件打包约 45 MiB（具体大小以文件为准）。

已覆盖：

- Python stdout / stderr、正常结束、非零退出码 Failed、CREATE_NO_WINDOW
- 5,000 行日志上限与中文 / 无效编码处理
- 重复启动不产生第二个脚本进程、停止、重启
- Python 父子进程树强制停止，父进程正常退出后不遗留子进程
- BAT/CMD 空格路径、空格参数、BAT 调用 Python 的进程树停止
- Node.js 实际输出、EXE 直接运行
- JSON 原子保存、备份恢复、重复 ID 和损坏配置保护
- 配置窗口必填字段、Windows 参数 roundtrip、自动解释器 / 工作目录更新
- 新增 / 编辑 / 删除，以及保存失败时不提交界面变更
- 运行中禁止编辑 / 删除，状态与行按钮同步
- 主窗口关闭只隐藏、托盘图标保留、双击信号恢复、确认退出完成

打包修复：开发 Python 附带的 ICU78 库使用版本化导出，不能满足 Qt 所需的 Windows ICU 导出。最终 spec 排除这些冲突库及旧 API 转发库，使用 Windows 10/11 系统库；实际 EXE 启动检查已通过。

边界：Windows 10 未在独立机器实测；托盘生命周期使用实际 Windows 托盘对象并发出其双击信号验证，不是人工双击测试。BAT/CMD 中带引号、百分号等命令字符的路径 / 参数不支持，详见 README。输出按 UTF-8 解码；日志为内存日志，重启应用不会保留。退出会强制停止全部脚本，避免后台残留。
